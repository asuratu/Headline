<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heima.mapper.HeadlineMapper">

    <resultMap id="BaseResultMap" type="com.heima.pojo.Headline">
        <id property="hid" column="hid" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="article" column="article" jdbcType="VARCHAR"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
        <result property="publisher" column="publisher" jdbcType="INTEGER"/>
        <result property="pageViews" column="page_views" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="version" column="version" jdbcType="INTEGER"/>
        <result property="isDeleted" column="is_deleted" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        hid,title,article,
        type,publisher,page_views,
        create_time,update_time,version,
        is_deleted
    </sql>
    <select id="selectAllByHid" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from headline
        where
        hid = #{hid,jdbcType=NUMERIC}
    </select>

    <select id="selectPageMap" resultType="NewsListItem">
        select hid,title,type,page_views pageViews,TIMESTAMPDIFF(HOUR,create_time,NOW()) pastHours,
        publisher from news_headline where is_deleted=0
        <if test="typeNewsReq.keyWords !=null and typeNewsReq.keyWords.length()>0 ">
            and title like concat('%',#{typeNewsReq.keyWords},'%')
        </if>
        <if test="typeNewsReq.type != null and typeNewsReq.type != 0">
            and type = #{typeNewsReq.type}
        </if>
    </select>

    <!--  浏览量增加  -->
    <update id="addViews">
        update news_headline
        set page_views = page_views + 1
        where hid = #{hid,jdbcType=NUMERIC}
    </update>
</mapper>
